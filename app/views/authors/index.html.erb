<h1>Authors</h1>

<% @authors.each do |author| %>
  <h2><%= author.name %></h2>
  <ul>
    <% author.posts.each do |post| %>
      <li><%= post.title %> - <%= post.tags.map(&:name).join(", ") %></li>
    <% end %>
  </ul>
<% end %>
